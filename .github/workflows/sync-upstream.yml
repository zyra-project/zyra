name: Sync from NOAA-GSL (main & staging → mirror/*, workflows stripped + LFS mirrored)

on:
  schedule:
    - cron: "*/30 * * * *"     # every 30 minutes
  workflow_dispatch:
    inputs:
      branches:
        description: "Space-separated branches to mirror from upstream"
        required: false
        default: "main staging"
      push_tags:
        description: "Also mirror tags? (true/false). Defaults to false to avoid release jobs."
        required: false
        default: "false"
      force:
        description: "Force push? (true/false)"
        required: false
        default: "true"

permissions:
  contents: write   # allows GITHUB_TOKEN to push to THIS repo

concurrency:
  group: mirror-zyra
  cancel-in-progress: false

jobs:
  sync:
    name: Mirror upstream to mirror/* (workflows stripped + LFS mirrored)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      BRANCHES: ${{ github.event.inputs.branches || 'main staging' }}
      PUSH_TAGS: ${{ github.event.inputs.push_tags || 'false' }}
      FORCE: ${{ github.event.inputs.force || 'true' }}
      UPSTREAM_URL: https://github.com/NOAA-GSL/zyra.git
      # Use canonical (case-stable) URL to avoid redirect noise
      MIRROR_URL: https://github.com/Hackshaven/zyra.git

    steps:
      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Configure Git
        run: |
          git config --global user.name "zyra-sync-bot"
          git config --global user.email "zyra-sync-bot@example.com"
          git config --global advice.detachedHead false

      # --- LFS support (install) ---
      - name: Install Git LFS (if needed)
        run: |
          if ! command -v git-lfs >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y git-lfs
          fi
          git lfs install

      - name: Clone upstream as a mirror
        run: |
          set -euo pipefail
          git clone --mirror "$UPSTREAM_URL" upstream.git

      - name: Strip upstream workflow files from history (prevent running their actions)
        run: |
          set -euo pipefail
          cd upstream.git
          # Install git-filter-repo (available via pip on the runner)
          python -m pip install --user git-filter-repo
          # Remove all files under .github/workflows from the repo history
          git filter-repo --path .github/workflows --invert-paths --force

      - name: Push selected branches (and optional tags) to mirror/*  (with LFS)
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail

          BRANCHES="${BRANCHES:-main staging}"
          PUSH_TAGS="${PUSH_TAGS:-false}"
          FORCE="${FORCE:-true}"
          FORCE_FLAG=""
          [ "$FORCE" = "true" ] && FORCE_FLAG="--force"

          cd upstream.git

          # git filter-repo drops remotes; recreate them now
          git remote add origin "$UPSTREAM_URL" || git remote set-url origin "$UPSTREAM_URL"
          git remote add mirror "https://x-access-token:${APP_TOKEN}@${MIRROR_URL#https://}" || \
            git remote set-url mirror "https://x-access-token:${APP_TOKEN}@${MIRROR_URL#https://}"

          # Ensure latest & prune deleted refs from upstream
          git fetch --prune origin

          # --- LFS support (mirror blobs as well) ---
          # 1) Pull all LFS objects referenced in upstream
          git lfs fetch origin --all || true
          # 2) Push all LFS objects into the downstream mirror's LFS store
          git lfs push mirror --all || true

          # For each requested branch, push only if SHA differs (skip no-ops)
          for b in $BRANCHES; do
            if git show-ref --verify --quiet "refs/heads/$b"; then
              UP_SHA=$(git rev-parse "refs/heads/$b")
              MIR_SHA=$(git ls-remote mirror "refs/heads/mirror/$b" | awk '{print $1}' || true)
              if [ -n "${MIR_SHA}" ] && [ "$UP_SHA" = "$MIR_SHA" ]; then
                echo "mirror/$b is up-to-date ($UP_SHA); skipping push."
              else
                echo "Pushing $b → mirror/$b ($UP_SHA != ${MIR_SHA:-none})"
                git push mirror "refs/heads/$b:refs/heads/mirror/$b" $FORCE_FLAG
              fi
            else
              echo "Skip: upstream branch '$b' not found."
            fi
          done

          # Tags are where many release workflows trigger; default is OFF.
          if [ "$PUSH_TAGS" = "true" ]; then
            echo "Pushing tags (note: upstream workflows have been stripped)."
            git fetch --tags origin
            git push mirror --tags $FORCE_FLAG
          else
            echo "Skipping tags (push_tags=false)."
          fi

      - name: Summary
        if: always()
        run: echo "Mirror (workflows stripped + LFS mirrored) complete at $(date -u +"%Y-%m-%dT%H:%M:%SZ")."
